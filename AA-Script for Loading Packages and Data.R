# This script loads the packages and data we'll need for the analyis. It also
# gives us a look at the main, combined dataset. 

library(tidyverse)
library(ggplot2)
wd(ST597 Final Project)
load('DataSet.RData')